"""Key LaTeX formulas for ESL Ch. 2 dashboard pages."""

from __future__ import annotations

OLS_OBJECTIVE = r"\hat{\beta} = \arg\min_\beta \|y - X\beta\|_2^2"
OLS_SOLUTION = r"\hat{\beta} = (X^\top X)^{-1} X^\top y \quad\text{(when } X^\top X \text{ is invertible)}"
