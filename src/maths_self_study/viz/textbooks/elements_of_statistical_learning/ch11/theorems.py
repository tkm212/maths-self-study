"""Theorems for ESL Ch. 11 dashboard pages."""

from __future__ import annotations

NEURAL_NETWORKS = [
    (
        "Universal approximation",
        r"A single hidden layer with sufficiently many sigmoid units can approximate any continuous function on a compact set — but gives no bound on $M$ (ESL §11.5.4).",
    ),
]

PROJECTION_PURSUIT = [
    (
        "Additive decomposition",
        r"PPR expresses $f(X)$ as a sum of $M$ univariate ridge functions — each term captures variation along one learned direction (ESL §11.2).",
    ),
]
